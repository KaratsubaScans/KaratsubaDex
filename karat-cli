#!/usr/bin/make -f

## config variables
SITE_URL:=https://karatsubascans.com

## no trailing slash in dirs
CONTENT_DIR:=content
RSS_DIR:=feeds
OUT_DIR:=manga
TEMPLATE_DIR:=templates

help:
	$(info karat-cli help|build)

init:
	echo "init"

build:
	echo "build"
	# for manga in $(CONTENT_DIR)/*; do \
	# 	cat "$$manga"; \
	# done

$(OUT_DIR)/%.html: $(CONTENT_DIR)/%.html
	cat $<

$(RSS_DIR)/%.xml:
	echo "feeds"

